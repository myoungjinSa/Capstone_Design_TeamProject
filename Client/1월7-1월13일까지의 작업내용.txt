1. 유니티 cs를 통한 파일 입출력 작업을 시도해봤다. (1/7~1/10)
-cs파일의 text버젼-> fbx로더폴더에. LabProject7-9에서 로드하고 정점정보가 잘 읽히긴 했지만 텍스트 x(존재x). 
Texture에 알비도맵 연결했지만 이름은 나오나 갯수는 카운트되지않음 --> unity cs공부해서 renderer로 부모생성자 바꿔주면서 nTexture확인했지만 x

-binary 파일 7-9-4에서 로드안됌. 첫번째로 이유가 nTexture일줄알고 텍스쳐와 같은 방식으로 바꿔봄 --> 실패
text파일처럼 형식 바꿔줌 x

2. maxScript 로 모델 정보 position, uv, normal,정보 추출(1/7~1/10- 1번과 역할을 나눠 동시 진행했음)
- 하지만 tangent값을 뽑는 방법이 인터넷상에서 찾을수 없어 다른 방법이 정 없을때 다시 시도 해보기로 정했다.


3. sdk를 다이렉트x 프레임워크에 쉽게 임포트하게 하기 위해 파일에 데이터 쓰는 순서를 수정하였다.
 다이렉트 x에서 해당 파일을 임포트하고 추출한 데이터를 이용하여 모델을 띄우는 데 성공하였다.
하지만 아직 텍스쳐는 입히지는 못했다. (1/10일)

4. fbxsdk 
sdk를 사용해서 fbx를 로드 할때 되는 모델이 있고 안되는 모델이 있는데 어떤 모델의 데이터가 있을때
추출 가능한지 연구. 

그 패턴을 찾아냈다. 현재 갖고 있는 sdk는 모델의 스킨메시가 하나만 존재하여야 하고(즉,무기를
들고 있는 캐릭터같은경우 무기를 삭제해줘야 됨), 
그 스킨메쉬에대한 애니메이션 정보가 하나만 존재할때 sdk를 사용해서 데이터 추출이 가능한것을 발견했다. (1/11일 )
fbxsdk를 이용해 캐릭터 애니메이션 정보를 추출할 수 있는 확률이 70프로정도 된다. (15% -> 70%)

5. 믹사모 홈페이지에서 캐릭터와 애니메이션을 다운로드 받아서
 3디 맥스로 1차 가공하여 sdk를 이용할 수 있게 추출하는 작업을 실시후 sdk로 fbx로딩
 우리가 원하는 position,uv,normal,blendWeights,blendIndices를 추출할수 있게 됐다.
 애니메이션 정보로는 AnimationClip,BoneHierarchy,BoneoffsetMatrix를 추출할수 있었다.
- 그러나 모든 캐릭터가 다 되는것이 아닌만큼 캐릭터 결정에 신중해야할것 같다. (1/11일)

6. 다이렉트 X에서 로드한 모델에 AlbedoTexture를 입히는데 성공했다. (1/11일)
   하지만 한 모델에 2개이상의 텍스쳐를 입혀지지 않는 문제가 발생. 

7. AlbedoTexture,NormalTexture,SpecularTexture를 한 모델에 다 입히고자 시도(1/12일)

8. 루나의 애니메이션 예제 코드 분석후, 필요하다 싶은 부분 참고. SkinnedData,AnimationClip,BoneAnimation,KeyFrames 구조체 정의( 1/13일)



 


